<% include partials/header.ejs %>
    <div class="table-wrapper" >
        <% if (package.length > 0) {%>
            <table class="table table-hovered">
                <thead class="thead-dark" >
                    <tr>
                        <th scope="col">PID</th>
                        <th scope="col">CID</th>
                        <th scope="col">Price</th>
                        <th scope="col">Pic</th>
                        <th scope="col">Details</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% package.forEach((pack, index) => { %>
                        <tr>
                            <th scope="row"><%= pack.packid %></th>
                            
                            <th scope="row"><%= pack.catid %></th>
                            <th scope="row"><%= pack.price %></th>
                            <th scope="row"><%= pack.pic %></th>
                            <th scope="row"><%= pack.details%></th>

<td scope="row"><a href="/" target="_blank" rel="noopener" class="btn btn-sm btn-success">EDIT</a>

<a href="/s>" target="_blank" rel="noopener" class="btn btn-sm btn-danger">DELETE</a>
</td>                               
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center">No players found. Go <a href="/add" >here</a> to add players.</p>
        <% } %>
    </div>
</div>

</html>