<% include partials/header.ejs %>
    <div class="table-wrapper" >
        <% if (customer.length > 0) {%>
            <table class="table table-hovered">
                <thead class="thead-dark" >
                    <tr>
                        <th scope="col">CID</th>
                        <th scope="col">NAME</th>
                        <th scope="col">PHNO</th>
                        <th scope="col">EMAIL</th>
                        <th scope="col">NUMBER</th>
                        <th scope="col">PID</th>

                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% customer.forEach((cust, index) => { %>
                        <tr>
                            <th scope="row"><%= cust.custid %></th>
                            
                            
                            <th scope="row"><%= cust.name %></th>
                            <th scope="row"><%= cust.phno %></th>
                            <th scope="row"><%= cust.email%></th>
                            <th scope="row"><%= cust.number%></th>
                            <th scope="row"><%= cust.packid%></th>
                            


<td scope="row"><a href="/" target="_blank" rel="noopener" class="btn btn-sm btn-success">EDIT</a>

<a href="/>" target="_blank" rel="noopener" class="btn btn-sm btn-danger">DELETE</a>
</td>                               
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center">No players found. Go <a href="/add" >here</a> to add players.</p>
        <% } %>
    </div>
</div>

</html>